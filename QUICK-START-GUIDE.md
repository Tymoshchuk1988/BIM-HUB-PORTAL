# 🚀 ШВИДКИЙ СТАРТ BIM HUB PORTAL

## 📋 ПОТОЧНИЙ СТАТУС:
✅ Сайт працює: https://bimhub.site  
✅ PHP працює: версія 7.3.33  
✅ База даних: створена на хостингу  
❌ Підключення до БД: потрібно налаштувати  
❌ Автентифікація: потрібно створити  

## 🎯 НЕГАЙНІ КРОКИ:

### КРОК 1: ПЕРЕВІРИТИ ПІДКЛЮЧЕННЯ ДО БАЗИ
1. Створіть файл test-db.php у корені сайту через cPanel File Manager
2. Вставте код тесту з test-db-connection.php
3. Відкрийте: https://bimhub.site/test-db.php
4. Перевірте результат

### КРОК 2: СТВОРИТИ КОНФІГУРАЦІЙНИЙ ФАЙЛ
1. Завантажте config-production.php як config.php
2. Розташування: /home/ec606796/bimhub.site/www/config.php

### КРОК 3: СТВОРИТИ ТАБЛИЦІ БАЗИ ДАНИХ
Створіть файл create-tables.php:

<?php
require_once 'config.php';
$db = getDB();

if($db) {
    $sql = "
    CREATE TABLE IF NOT EXISTS users (
        id INT AUTO_INCREMENT PRIMARY KEY,
        email VARCHAR(255) UNIQUE NOT NULL,
        password_hash VARCHAR(255) NOT NULL,
        full_name VARCHAR(255),
        role ENUM('admin','manager','viewer') DEFAULT 'viewer',
        status ENUM('active','inactive') DEFAULT 'active',
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    
    CREATE TABLE IF NOT EXISTS projects (
        id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(255) NOT NULL,
        description TEXT,
        status VARCHAR(50) DEFAULT 'planning',
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    ";
    
    try {
        $db->exec($sql);
        echo "✅ Таблиці створені!";
    } catch(Exception $e) {
        echo "❌ Помилка: " . $e->getMessage();
    }
}
?>

### КРОК 4: СТВОРИТИ СИСТЕМУ АВТЕНТИФІКАЦІЇ
1. Створіть login.php
2. Створіть logout.php  
3. Створіть dashboard.php

## 🔧 ДАНІ ДЛЯ РОБОТИ:

### cPanel:
- URL: https://ec606796.ftp.tools:2083
- Логін: ec606796
- Пароль: Tymoshchuk1988

### База даних:
- Хост: ec606796.mysql.tools
- База: ec606796_bimhub
- Користувач: ec606796_bimhub
- Пароль: (9ypA;7Ha6

### Файловий менеджер:
- Шлях: /home/ec606796/bimhub.site/www

## 🚀 ШВИДКИЙ ПЛАН:

### СЬОГОДНІ:
1. Налаштувати підключення до БД ✅
2. Створити таблиці
3. Додати тестових користувачів

### ЗАВТРА:
1. Створити систему входу/виходу
2. Створити панель управління
3. Додати управління проектами

### ЦЬОГО ТИЖНЯ:
1. Створити REST API
2. Додати React компоненти
3. Налаштувати CI/CD для продакшена

## ⚠️ ВАЖЛИВО:
1. Після налаштування видаліть test-db.php та create-tables.php
2. Змініть паролі тестових користувачів
3. Налаштуйте .htaccess для захисту

## 📞 ПІДТРИМКА:
Якщо виникнуть проблеми:
1. Перевірте права доступу до файлів (755/644)
2. Перевірте підключення до БД
3. Перевірте логи PHP у cPanel
